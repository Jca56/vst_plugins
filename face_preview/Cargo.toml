[package]
name = "face_preview"
version = "0.1.0"
edition = "2021"
authors = ["Alva"]
description = "Screenshot Lantern plugin faces to PNGs natively — no host, no Wine, no Ableton relaunch"

[dependencies]
lantern_ui = { path = "../lantern_ui" }
lantern_vst3 = { path = "../lantern_vst3" }
lantern_keylight = { path = "../lantern_keylight" }
lantern_compressor = { path = "../lantern_compressor" }
lantern_eq = { path = "../lantern_eq" }
